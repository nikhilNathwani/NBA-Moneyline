[pytest]
# Puts this directory on sys.path (matching how main.py is launched, since
# it's run from here too), so tests can use the same `from scrape.xxx import ...`
# / `from storage.xxx import ...` / `from util.xxx import ...` style imports
# as the rest of the codebase without needing package installation.
pythonpath = .
